{{#if session.isAuthenticated}}
<div class="jumbo">
<h1>Zdravo {{ session.data.authenticated.korisnik.korisnickoIme }} !</h1>
{{log model.brojOglasa}}
<br>
{{#if (lte model.brojOglasa.broj 2)}}
{{#link-to 'advertise' tagName='button' class="btn btn-primary"}} Objavi novi oglas{{/link-to}}
{{else}}
{{#link-to 'advertise' tagName='button' class="btn btn-primary"}} Objavi novi oglas{{/link-to}}
<h5>Objavili ste vec tri besplatna oglasa, svaki naredni se placa!</h5>
{{/if}}
<br> <br>
<a  href="" class="btn btn-primary" {{action 'deleteProfile'}}>Obrisi profil</a>
</div>
<br>
<div class="jumbo">
  <h1>Moji oglasi</h1>

  <h4> {{model.brojOglasa.broj}} </h4>
  {{log model.rentals}}
    {{#if model.rentals}}
  {{#each model.rentals as |rental|}}

    <article class="listing">
    {{#if rental.slika }}
      <img src="{{rental.slika}}" alt="">
    {{else}}
     <img src="http://www.manufacturenet.ca/wp-content/uploads/2016/09/real_Estate.jpg" alt="">
    {{/if}}
      <h3>{{#link-to 'rentals.show' rental.id}}{{rental.nazivOglasa}}{{/link-to}}</h3>

      <div class="detail owner">
        <span>Vlasnik:</span> {{rental.korisnik.ime}}
      </div>
      <div class="detail type">
        <span>Tip:</span> {{rental.vrstaOglasa}}
      </div>
      <div class="detail location">
        <span>Kvadratura:</span> {{rental.kvadratura}}
      </div>
      <div class="detail bedrooms">
        <span>Broj prostorija>:</span> {{rental.brojProstorija}}
      </div>

      <div class="detail location">
            <span>Adresa:</span> {{rental.grad}}
          </div>

      <div class="detail price">
        <span>Cijena:</span> {{rental.cijena}} BAM
      </div>
      </article>

  {{/each}}
  {{else}}
  <h3>Nemate kreiranih oglasa! </h3>
  {{/if}}
</div>
{{/if}}
